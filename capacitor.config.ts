import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.pbz.app',
  appName: 'PickleBallZ Sagas',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
